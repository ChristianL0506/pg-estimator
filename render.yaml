services:
  - type: web
    name: picou-group-estimator
    runtime: docker
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: pg-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
